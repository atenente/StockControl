<div class="container">
  <h1><%= t(".detail_ company") %></h1>
  <div class="table-responsive-md">
    <table class="table">
      <%= render 'header_company' %>
      <%= render @company %>
    </table>
  </div>
  <div class="d-flex justify-content-between">
    <%= link_to t('global.back'), companies_path, class: 'btn btn-primary' %>
    <%= button_to t('global.delete'), @company, method: :delete, class: 'btn btn-danger' %>
  </div>
</div>
