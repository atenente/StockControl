<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Stock Control', root_path, class: 'navbar-brand' %>
    <% if user_signed_in? %>
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to t('global.home'), root_path, class: 'nav-link active text-primary' %>
        <li class="nav-item">
          <%= link_to t('products.products'), products_path, class: 'nav-link active text-primary' %>
        </li>
        <% if current_user&.admin? %>
          <li class="nav-item">
            <%= link_to t('companies.companies'), companies_path, class: 'nav-link active text-primary' %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
            <li>
              <%= link_to t('devise.shared.links.edit_profile'), edit_user_registration_path, class: 'dropdown-item' %>
            </li>
            <li>
              <%= link_to t('devise.shared.links.sign_out'), destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            </li>
          </ul>
        </li>
      </ul>
    <% end %>
  </div>
</nav>
