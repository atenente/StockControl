<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">Stock Control</a>
      <ul class="navbar-nav me-auto flex-row list-group-color blue">
          <li class="nav-item">
            <a class="nav-link active text-primary"  href="<%= root_path %>">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active text-primary"  href="<%= products_path %>"><%= t('products.products') %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link active text-primary"  href="<%= companies_path %>"><%= t('companies.companies') %></a>
          </li>
      </ul>
      <ul class="nav justify-content-end">
        <% if user_signed_in? %>
          <%= link_to current_user.email, edit_user_registration_path, class: "nav-item nav-link" %>
          <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete} %>
        <% else %>
          <%= link_to "Sign up", new_user_registration_path, class: "nav-item nav-link" %>
          <%= link_to "Login", new_user_session_path, class: "nav-item nav-link" %>
        <% end %>
      </ul>
  </div>
</nav>
