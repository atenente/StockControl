<div class="navbar-top-line bg-dark border-bottom border-secondary">
  <div class="container-fluid text-center">
    <span class="text-light"><i class="bi bi-ui-checks-grid"> </i> Stock Control</span>
  </div>
</div>
<nav class="navbar navbar-expand-md navbar-dark bg-dark p-0">
  <div class="container-fluid">
    <% if user_signed_in? %>
      <%= link_to "#{current_user.company.name}", root_path, class: 'navbar-brand' %>
      <ul class="nav nav-tabs me-auto border-bottom-0">
        <li class="nav-item">
          <%= link_to t('global.home'), root_path, class: "nav-link #{'active' if current_page?(root_path)}" %>
        <li class="nav-item">
          <%= link_to t('products.products'), products_path, class: "nav-link #{'active' if controller_name == 'products'}" %>
        </li>
        <li class="nav-item">
          <%= link_to t('invoices.invoices'), invoices_path, class: "nav-link #{'active' if controller_name == 'invoices'}" %>
        </li>
        <% if current_user&.admin? %>
          <li class="nav-item">
            <%= link_to t('companies.companies'), companies_path, class: "nav-link #{'active' if controller_name == 'companies'}" %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
            <li>
              <%= link_to t('devise.shared.links.edit_profile'), edit_user_registration_path, class: 'dropdown-item' %>
            </li>
            <li>
              <%= link_to t('devise.shared.links.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'dropdown-item' %>
            </li>
          </ul>
        </li>
      </ul>
    <% end %>
  </div>
</nav>
